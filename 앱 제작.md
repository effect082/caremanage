# 일일 케어 현황 앱 제작 기획서 (바이브 코딩용)

> 이 문서 하나만으로 "일일 케어 현황" 앱을 완성도 높게 제작할 수 있도록 작성된 통합 기획서입니다.
> AI 코딩 도구(Claude Code, Cursor 등)에 이 MD 파일을 그대로 입력하여 앱 제작을 요청하세요.

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 앱 이름 | 일일 케어 현황 (Daily Care Status) |
| 목적 | 요양보호사가 거동이 불편한 어르신을 케어한 내용을 매일 기록하고, 가족이 언제든 실시간으로 확인할 수 있도록 지원 |
| 핵심 가치 | 신뢰(가족 안심), 간편함(요양보호사 입력 편의성), 접근성(모바일 최적화) |
| 사용자 유형 | ① 요양보호사(작성자) ② 가족(열람자) |
| 배포 방식 | GitHub Pages(프론트) + GAS(백엔드, 구글 스프레드시트 DB) + Vercel(최종 배포) |

---

## 1. 사용자 역할 및 인증 (회원가입/로그인)

### 1.1 회원 구분
- **요양보호사**: 일일 케어 기록을 "작성"하는 대시보드 접근
- **가족**: 요양보호사가 작성한 기록을 "열람"하는 관리 현황 대시보드 접근

### 1.2 회원가입 입력 항목
| 필드 | 형식 | 비고 |
|---|---|---|
| 이름 | 문자열 | 필수 |
| 구분 | 라디오 선택 (요양보호사 / 가족) | 필수, 역할에 따라 화면 분기 |
| 비밀번호 | 숫자 4자리 | 필수, 간단한 PIN 방식 (숫자 키패드 UI 권장) |

### 1.3 로그인 방식
- 이름 + 구분 + 4자리 숫자 비밀번호로 로그인
- 로그인 성공 시 역할(요양보호사/가족)에 따라 자동으로 해당 대시보드로 라우팅
  - 요양보호사 → "일일 케어 현황 작성 대시보드"
  - 가족 → "관리 현황 대시보드"
- 비밀번호는 4자리 숫자이므로 평문 저장을 지양하고, 최소한 간단한 해시(SHA-256 등) 처리 후 스프레드시트에 저장 권장
- 로그인 상태 유지는 브라우저 localStorage/sessionStorage 활용 (자동 로그아웃 시간 설정 가능)

### 1.4 가족-어르신 연결 구조
- 한 명의 어르신에 대해 여러 명의 가족 구성원이 열람 가능해야 함
- 요양보호사 1명 : 어르신 1명 : 가족 N명 구조를 기본으로 설계
- 회원가입 시 "담당 어르신" 또는 "연결 코드"를 입력받아 매칭 (예: 어르신별 고유 코드 발급)

---

## 2. 핵심 기능 요구사항

### 2.1 하루 2회 혈압·체온 체크 (아침/저녁)
- 입력 시점: 아침, 저녁 각각 1회씩 (하루 총 2세트)
- 입력 항목
  - 수축기 혈압 (mmHg)
  - 이완기 혈압 (mmHg)
  - 체온 (℃, 소수점 1자리)
  - 측정 시간 (자동 기록, 수정 가능)
- 정상 범위를 벗어나는 수치 입력 시 경고 색상(빨간색) 표시 (예: 수축기 140 이상, 체온 37.5℃ 이상)
- 아침/저녁 입력 여부를 카드 형태로 구분 표시 (미입력 시 "미작성" 뱃지)

### 2.2 하루 컨디션 체크
- 하루 전체를 기준으로 상 / 중 / 하 3단계 선택 (버튼 또는 이모지 형태 UI 권장: 😀 상, 😐 중, 😟 하)
- 특이사항 메모란(텍스트, 선택 입력) 추가

### 2.3 하루 식사 정도 체크
- 잘 드심 / 보통 / 잘 못 드심 3단계 선택
- 끼니별(아침/점심/저녁) 입력이 필요한 경우 확장 가능하되, 기본은 "하루 1회 종합 체크"로 설계
- 메모란(선택 입력) 추가 (예: "죽만 반 공기 드심")

### 2.4 배변 상태 체크
| 항목 | 입력 방식 |
|---|---|
| 배변 횟수 | 숫자 입력 (0회 이상, +/- 버튼 UI 권장) |
| 배변 형태 | 딱딱함 / 부드러움 / 물처럼 묽음 (3단계 선택) |

### 2.5 일일 기록 요약 카드
- 하루 단위로 위 4가지(혈압·체온, 컨디션, 식사, 배변) 기록을 하나의 "일일 케어 카드"로 통합하여 저장/표시
- 요양보호사는 하루 1개의 카드를 아침/저녁에 걸쳐 채워나가는 구조

---

## 3. 화면(대시보드) 설계

### 3.1 요양보호사 - 일일 케어 현황 작성 대시보드
1. **상단**: 오늘 날짜, 어르신 이름, 로그아웃 버튼
2. **입력 섹션 (탭 또는 아코디언 구조)**
   - 아침 체크 (혈압/체온)
   - 저녁 체크 (혈압/체온)
   - 오늘의 컨디션
   - 오늘의 식사
   - 오늘의 배변
3. **저장 버튼**: 항목별 즉시 저장(자동 저장) 또는 하단 "오늘 기록 저장" 버튼
4. **하단**: 최근 7일간 작성 이력 미리보기(달력 형태, 작성 완료일은 체크 표시)

### 3.2 가족 - 관리 현황 대시보드
1. **상단**: 어르신 이름, 오늘 날짜, 오늘 작성 완료 여부(뱃지: "오늘 기록 완료" / "미작성")
2. **오늘의 케어 요약 카드**
   - 아침/저녁 혈압·체온 (그래프 또는 카드 형태)
   - 컨디션 상태 (아이콘)
   - 식사 상태 (아이콘)
   - 배변 상태 (횟수 + 형태)
3. **주간/월간 추이 그래프**: 혈압·체온 변화 추이를 꺾은선 그래프로 시각화 (Chart.js 또는 Recharts 권장)
4. **달력 뷰**: 날짜별 기록 여부 및 컨디션(상/중/하)을 색상으로 한눈에 확인
5. **날짜 클릭 시 상세 기록 팝업(모달)**로 해당 일자 전체 케어 기록 확인

### 3.3 공통 화면
- 로그인 화면 (이름 + 구분 선택 + 4자리 숫자 키패드)
- 회원가입 화면
- 하단 네비게이션 바 또는 하단 탭바 (모바일 기준 엄지손가락으로 조작 가능한 위치)

---

## 4. 데이터 구조 (구글 스프레드시트 기준 설계)

### 4.1 시트 구성
**Sheet 1. Users (회원 정보)**
| 컬럼 | 설명 |
|---|---|
| user_id | 고유 ID (자동 생성) |
| name | 이름 |
| role | 요양보호사 / 가족 |
| password_hash | 4자리 숫자 비밀번호 해시값 |
| elder_code | 연결된 어르신 고유 코드 |
| created_at | 가입일시 |

**Sheet 2. Elders (어르신 정보)**
| 컬럼 | 설명 |
|---|---|
| elder_code | 고유 코드 |
| elder_name | 어르신 이름 |
| caregiver_id | 담당 요양보호사 user_id |

**Sheet 3. DailyCare (일일 케어 기록)**
| 컬럼 | 설명 |
|---|---|
| record_id | 고유 기록 ID |
| elder_code | 어르신 코드 |
| date | 기록 날짜 (YYYY-MM-DD) |
| morning_systolic / morning_diastolic / morning_temp / morning_time | 아침 혈압·체온·측정시간 |
| evening_systolic / evening_diastolic / evening_temp / evening_time | 저녁 혈압·체온·측정시간 |
| condition | 상 / 중 / 하 |
| condition_memo | 컨디션 메모 |
| meal_status | 잘 드심 / 보통 / 잘 못 드심 |
| meal_memo | 식사 메모 |
| stool_count | 배변 횟수 |
| stool_type | 딱딱함 / 부드러움 / 물처럼 묽음 |
| updated_by | 작성한 요양보호사 user_id |
| updated_at | 최종 수정 시각 |

### 4.2 GAS(Google Apps Script) 웹앱 API 설계
- GAS를 웹 앱으로 배포하여 REST API처럼 사용 (doGet / doPost)
- 주요 엔드포인트 (쿼리 파라미터 action 기준으로 분기 처리 권장)
  - `action=signup` : 회원가입 처리
  - `action=login` : 로그인 검증
  - `action=getDailyCare&elder_code=&date=` : 특정 날짜 기록 조회
  - `action=saveDailyCare` : 일일 기록 저장/수정 (POST)
  - `action=getMonthlyCare&elder_code=&month=` : 월간 기록 조회 (달력/그래프용)
- 응답 형식: JSON
- CORS 이슈 방지를 위해 GAS 배포 시 "액세스 권한: 모든 사용자"로 설정
- 프론트엔드에서는 `fetch()`로 GAS 웹앱 URL 호출

> **[작성 완료]** GAS 웹 앱 URL: `https://script.google.com/macros/s/AKfycbxaF4BbsWz40cd2iGd_EENS1xCnufLl41BfSNNcJsZBu703Xuq_1rYxzRc6rMyTEFrZ/exec`

---

## 5. 기술 스택

| 영역 | 기술 |
|---|---|
| 프론트엔드 | HTML, CSS, JavaScript (또는 React) |
| 폰트 | Pretendard 계열이 아닌 **페이퍼로지(Paperlogy)** 폰트 적용 (웹폰트 CDN 또는 자체 호스팅) |
| 그래프 | Chart.js 또는 Recharts |
| 백엔드 | Google Apps Script (GAS) + Google Sheets |
| 배포(1) | GitHub Pages |
| 배포(2, 최종) | Vercel |
| 버전 관리 | GitHub |

---

## 6. GitHub / Vercel 연동 및 배포 절차

### 6.1 GitHub 연동
1. GitHub 저장소(Repository) 생성
2. 앱 소스코드 전체를 저장소에 업로드 (main 브랜치 기준)
3. GitHub Pages 설정
   - Settings → Pages → Source: `main` 브랜치, 폴더: `/root` (또는 `/docs`, 프로젝트 구조에 맞게 지정)
4. > **[작성 필요]** GitHub 저장소 URL: `여기에 입력`
5. > **[작성 필요]** GitHub Pages 배포 URL: `여기에 입력`

### 6.2 Vercel 배포
1. Vercel 계정에서 "New Project" → 위에서 만든 GitHub 저장소 Import
2. 빌드 설정
   - Framework Preset: 정적 사이트일 경우 "Other" 또는 "Static", React/Next.js 사용 시 해당 프레임워크 자동 감지
   - Output Directory: 프로젝트 구조에 맞게 지정
3. 환경변수(Environment Variables)에 GAS 웹앱 URL 등록 (예: `NEXT_PUBLIC_GAS_URL` 또는 `VITE_GAS_URL`)
4. Deploy 클릭 후 자동 배포 URL 발급
5. > **[작성 필요]** Vercel 배포 URL: `여기에 입력`

### 6.3 배포 흐름 요약
```
GitHub(코드 저장) → GitHub Pages(1차 정적 배포/테스트)
                  → Vercel(GitHub 연동, 최종 프로덕션 배포)
                  → GAS 웹앱(백엔드 API) ↔ Google Sheets(DB)
```

---

## 7. UI/UX 디자인 가이드

### 7.1 디자인 컨셉
- **스타일**: 글래스모피즘(Glassmorphism) — 반투명 배경 + 블러(backdrop-filter: blur) + 은은한 테두리(1px, 반투명 흰색)
- **색상**
  - 메인: 흰색(#FFFFFF) + 파란색 계열(#2F6FED, #5B9BF8 등)
  - 과도한 그라데이션 사용 금지 → 단색 위주, 포인트 색상만 최소한으로 사용
  - 경고/이상 수치 표시용 보조 색상만 예외적으로 붉은 계열(#E5484D) 허용
- **폰트**: 페이퍼로지(Paperlogy) 전체 페이지 적용 (제목/본문 웨이트 구분 사용)
- **아이콘**: 라인 아이콘 스타일(예: Lucide, Feather) 사용, 컨디션/식사 상태는 이모지 병행 가능

### 7.2 반응형 웹 구현
- **모바일 퍼스트(Mobile First)**로 설계 후 태블릿/PC로 확장
- 브레이크포인트 기준 예시
  - 모바일: ~480px
  - 태블릿: 481px ~ 1024px
  - PC: 1025px 이상
- 스마트폰 최적화 필수 사항
  - 하단 고정 탭 네비게이션(엄지 조작 영역 고려)
  - 입력 버튼/터치 영역 최소 44x44px 이상 확보
  - 숫자 입력은 키패드가 자동으로 뜨는 `inputmode="numeric"` 적용
  - 혈압/체온 등은 슬라이더 또는 +/- 스텝퍼 버튼으로 입력 편의성 강화
  - 스크롤 최소화를 위한 아코디언/탭 구조 적극 활용
- **직관성 원칙**
  - 한 화면에 한 가지 주요 작업만 배치
  - 아이콘 + 텍스트 병행 표기 (고령 사용자 및 다양한 연령대 가족 고려)
  - 글자 크기: 본문 최소 16px 이상 (모바일 가독성 확보)
  - 색맹/저시력 사용자를 고려해 색상 외에도 아이콘/텍스트로 상태 구분

### 7.3 주요 컴포넌트 스타일 예시 (CSS 참고 가이드)
```css
.glass-card {
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.4);
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(47, 111, 237, 0.08);
}

body {
  font-family: 'Paperlogy', sans-serif;
  background-color: #F5F8FF;
  color: #1A1A1A;
}

.btn-primary {
  background-color: #2F6FED;
  color: #FFFFFF;
  border-radius: 12px;
  min-height: 44px;
}
```

---

## 8. 개발 우선순위 (권장 진행 순서)

1. 회원가입/로그인 (요양보호사/가족 구분, 4자리 PIN)
2. GAS + 구글 스프레드시트 백엔드 API 구축 (Users, Elders, DailyCare 시트)
3. 요양보호사 대시보드 - 일일 기록 입력 기능 (혈압/체온 → 컨디션 → 식사 → 배변 순)
4. 가족 대시보드 - 오늘의 기록 조회 기능
5. 가족 대시보드 - 달력 뷰 및 주간/월간 추이 그래프
6. UI 디자인 적용 (글래스모피즘, 폰트, 색상, 반응형)
7. GitHub 업로드 → GitHub Pages 테스트 배포
8. Vercel 연동 및 최종 배포

---

## 9. 향후 확장 고려 사항 (선택)
- 이상 수치 발생 시 가족에게 알림(푸시/문자/카카오톡 알림톡) 발송 기능
- 여러 명의 어르신을 관리하는 요양보호사를 위한 다중 어르신 전환 기능
- 사진/동영상 첨부를 통한 케어 기록 보강
- 복약 체크리스트 기능 추가
- PDF 형태의 주간/월간 리포트 다운로드 기능

---

### 참고: [작성 완료] 항목 체크리스트
- [x] GitHub 저장소 URL: https://github.com/kohyunock/caremanage
- [x] GitHub Pages 배포 URL: https://kohyunock.github.io/caremanage/
- [x] GAS 웹 앱 URL: https://script.google.com/macros/s/AKfycbxaF4BbsWz40cd2iGd_EENS1xCnufLl41BfSNNcJsZBu703Xuq_1rYxzRc6rMyTEFrZ/exec
- [x] Vercel 배포 URL: https://caremanage.vercel.app
